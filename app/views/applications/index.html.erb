<div class="content">
  <h1>Applications to <%= @job.title %></h1>  
  <ul>
    <% @applications.each do |application| %>
      <li><%= link_to "#{application.user.email}", application_path(application), method: :get %></li>
    <% end %>
  </ul>
</div>
